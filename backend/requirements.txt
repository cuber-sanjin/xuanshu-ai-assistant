# ===== 玄枢后端依赖 =====
# 说明：每个依赖的用途
# fastapi          : Web 框架，提供路由/Pydantic校验/自动文档
# uvicorn[standard]: ASGI 服务器，运行 FastAPI；standard 版附带 uvloop/websockets
# langchain        : LLM 应用框架，提供工具、链、抽象层
# langchain-openai : 通过 OpenAI 兼容接口调用阿里云百炼 Qwen
# langgraph        : Agent 状态图编排（state/nodes/graph，本项目核心）
# python-dotenv    : 读取 .env 环境变量

fastapi>=0.141
uvicorn[standard]>=0.52
langchain>=1.3
langchain-openai>=1.6
langgraph>=0.5
python-dotenv>=1.0
pydantic>=2.8
pydantic-settings>=2.5
# SQLAlchemy: ORM 数据访问（Todo/Note/记忆持久化）
sqlalchemy>=2.0.52
# tzdata: Windows 下 zoneinfo 需要 IANA 时区库（Linux 自带，Windows 必装）
tzdata>=2024.1
# python-multipart: FastAPI 文件上传（语音 ASR 接口需要）
python-multipart>=0.0.9
# httpx: TTS 合成请求 + 音频下载（langchain 的传递依赖，此处显式声明）
httpx>=0.27
# pytest: 单元测试（calculator 安全、工具 CRUD 等）
pytest>=8.0
